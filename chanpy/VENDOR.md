# chanpy

[Vespa314/chan.py](https://github.com/Vespa314/chan.py) 源码，由 `src/app/services/chan/backend.py` 加入 `sys.path` 后 `import Chan`。

已剔除：`Debug/`、`App/`、`Image/`、`Script/`。升级时覆盖本目录并执行 `uv run pytest tests/test_chanpy_smoke.py`。
